# This workflow ensures that PR authors have acknowledged and reviewed the code suggestions made by the PR agent
# by checking if the author has marked a self-review checkbox in the comments.
# ugh

name: Verify Author Self-Review

on:
  pull_request:
    types: [opened, synchronize]
  # Since `issue_comment` cannot directly trigger a PR workflow, this must be done via the main branch workflow.
  issue_comment:
    types: [edited]

jobs:
  verify-author-self-review:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check for Author self-review in comments and Create or Update Check Run
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            console.log(JSON.stringify(context.payload, null, 2));
            const isPullRequest = context.payload.pull_request !== undefined;
            const issue_number = isPullRequest ? context.payload.pull_request.number : context.payload.issue.number;
            const ref = isPullRequest ? context.payload.pull_request.head.sha : context.payload.issue.pull_request.head.sha;

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issue_number
            });

            let foundChecked = false;
            let foundUnchecked = false;

            comments.forEach(comment => {
              if (!comment.deleted) {
                const body = comment.body.trim();
                if (body.includes('- [x]  **Author self-review**:')) {
                  foundChecked = true;
                } else if (body.includes('- [ ]  **Author self-review**:')) {
                  foundUnchecked = true;
                }
              }
            });

            console.log(`foundChecked: ${foundChecked}, foundUnchecked: ${foundUnchecked}`);

            const { data: checkRuns } = await github.rest.checks.listForRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: ref,
            });

            let checkRun = checkRuns.check_runs.find(run => run.name === 'Verify Author Self-Review' && run.head_sha === ref);

            const status = 'completed';
            let conclusion = 'failure';
            let summary = 'Author self-review is not checked. Please check the self-review checkbox in the Bot Review comment.';

            if (foundChecked) {
              conclusion = 'success';
              summary = 'Author self-review is checked.';
            }

            if (!checkRun) {
              await github.rest.checks.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                name: 'Verify Author Self-Review',
                head_sha: ref,
                status: status,
                conclusion: conclusion,
                output: {
                  title: 'Verify Author Self-Review',
                  summary: summary
                }
              });
              console.log('Check run created.');
            } else {
              await github.rest.checks.update({
                owner: context.repo.owner,
                repo: context.repo.repo,
                check_run_id: checkRun.id,
                status: status,
                conclusion: conclusion,
                output: {
                  title: 'Verify Author Self-Review',
                  summary: summary
                }
              });
              console.log('Check run updated.');
            }
